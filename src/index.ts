//import { S3 } from "aws-sdk";
//import { DATA_S3_BUCKET_NAME } from "./config";
//const s3 = new S3();

export const handler = async (): //event: S3Envent
Promise<string> => {
  // Check originator whitelist

  // Get metadata file

  // Check only single file PDF is attached

  // Send PDF to Textract and enqueue meta data

  return "Finished processing.";
};
